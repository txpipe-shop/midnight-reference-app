/**
 * Legacy placeholder retained for UI source compatibility. Sentinel v1 no
 * longer exposes the earlier rules ledger; sponsorship policy is configured
 * through the immutable campaign fields instead.
 */
export const Rules = () => (
  <pre className="p-4 bg-muted rounded-md text-sm font-mono overflow-auto max-h-[300px]">
    Rules moved to the sponsorship campaign policy.
  </pre>
);
